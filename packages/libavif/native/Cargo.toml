[package]
name = "libavif_native"
version = "0.1.0"
edition = "2021"
links = "libavif_native"

[lib]
crate-type = ["cdylib", "staticlib"]

[build-dependencies]
cc = "1.2"
cmake = "0.1"

[profile.release]
lto = true
codegen-units = 1
